# CI Environment Variables (no secrets)
# Copy this file to .env during CI runs

# Supabase configuration for CI/testing
# These should be test project credentials or mocked values
VITE_SUPABASE_PROJECT_ID="test-project-id"
VITE_SUPABASE_URL="https://test-project.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="test-publishable-key"

# Optional: Add other non-secret environment variables here
